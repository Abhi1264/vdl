package ui

const helpText = `Keyboard Shortcuts:

Enter     → Start download
↑ ↓ / j k → Navigate jobs
p         → Pause/cancel selected job
s         → Toggle SponsorBlock (YouTube only)
f         → Select download profile
?         → Toggle this help
q / Ctrl+C → Quit

Download Profiles:
Each profile shows exact technical specs and quality label.
Profiles apply to all new downloads.

SponsorBlock:
Automatically enabled for YouTube URLs.
Removes: sponsor, selfpromo, interaction, intro, outro segments.
`
